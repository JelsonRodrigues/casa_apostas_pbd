CREATE SCHEMA CASA_APOSTAS;
CREATE TABLE CASA_DE_APOSTA (
    ID_CASA_APOSTA  INTEGER NOT NULL PRIMARY KEY,
    NOME            VARCHAR(15),
    CNPJ            CHAR(18),
    DATA_CRIACAO    DATETIME,
    ENDERECO        VARCHAR(100),
    TELEFONE        VARCHAR(15)
);